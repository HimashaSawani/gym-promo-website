const Hero = () => {
  return (
    <section id="home" className="hero">
      <div className="hero-content">
        <h1>Train Strong. Live Better.</h1>
        <p>Performance-focused coaching and facilities for every level.</p>
        <a href="#contact" className="btn-primary">
          Start Today
        </a>
      </div>
    </section>
  );
};

export default Hero;
